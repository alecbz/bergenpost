<h1>All Articles</h1>
<%= '(' + (link_to 'create new', new_author_path) + ')'%>
<br /><br />
<ol>
  <% for article in @articles %>
    <li number="<%= article.id %>"><%= link_to(h(article.title),edit_article_path(article)) %></li>
  <% end %>
</ol>