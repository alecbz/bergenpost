<div class="block_content_left">
  <div class="block_inside">
    <h1><%= @months[@month.to_i] + ', ' + @year%></h1>
    <% if @articles.empty? %>
      No articles found.
    <% end %>
    <% @articles.each do |article| %>
      <%= link_to article.title, article %><br />
    <% end %>
  </div>
</div>
